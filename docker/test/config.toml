addr = "127.0.0.1"
port = 5000

[s3]
bucket = "test-bucket"
region = "us-east-1"
client_id = "minioadmin"
client_secret = "minioadmin"
host = "http://localhost:9000"

[local_fs]
root_path = "/tmp/test-local-fs"

[sqlite]
s3_object_path = "test.db"

[smtp]
host = "localhost"
port = 1025
use_tls = false
username = ""
password = ""
from_email = "noreply@example.com"